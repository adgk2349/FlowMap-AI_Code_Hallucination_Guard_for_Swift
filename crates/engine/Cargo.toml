[package]
name = "engine"
version = "0.1.0"
edition = "2021"

[dependencies]
protocol = { path = "../protocol" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tempfile = "3"
walkdir = "2"

[dev-dependencies]
