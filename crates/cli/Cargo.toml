[package]
name = "cli"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "flowmap"
path = "src/main.rs"

[dependencies]
engine = { path = "../engine" }
protocol = { path = "../protocol" }
serde_json = "1"
